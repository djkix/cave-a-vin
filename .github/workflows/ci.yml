name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  api:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env: { POSTGRES_USER: cave, POSTGRES_PASSWORD: cave, POSTGRES_DB: cave }
        ports: ["5432:5432"]
        options: --health-cmd "pg_isready -U cave" --health-interval 5s --health-timeout 3s --health-retries 10
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: --health-cmd "redis-cli ping" --health-interval 5s --health-timeout 3s --health-retries 10
    env:
      DATABASE_URL: postgresql://cave:cave@localhost:5432/cave
      REDIS_URL: redis://localhost:6379
      SESSION_SECRET: ci-secret-ci-secret-ci-secret-ci-secret
      BREAK_GLASS_EMAIL: ci@example.com
      BREAK_GLASS_PASSWORD: ci-break-glass-password
      ADMIN_EMAILS: ci@example.com
    defaults: { run: { working-directory: api } }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm, cache-dependency-path: api/package-lock.json }
      - run: npm ci
      - run: npx prisma migrate deploy
      - run: npx prisma generate
      - run: npm run lint
      # Le lint ne type-checke pas : sans ceci, une erreur de type dans un point
      # d'entrée, un module ou un contrôleur ne serait vue qu'au build de l'image.
      - run: npx tsc --noEmit -p tsconfig.build.json
      - run: npm test
      - run: npm run build

  web:
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: web } }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm, cache-dependency-path: web/package-lock.json }
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build

  # Dockge crée une stack à partir du seul docker-compose.yml et de son .env,
  # sans cloner le dépôt. Ce job reproduit exactement cette situation : si un
  # service dépend d'un fichier du dépôt monté depuis l'hôte, Docker monte un
  # dossier vide à la place et le service échoue en silence — c'est ainsi que la
  # sauvegarde n'a jamais tourné en production, en redémarrant en boucle avec le
  # code 0. Le job exige donc un vrai dump, pas seulement un conteneur démarré.
  compose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Stack autonome — sauvegarde réelle sans aucun fichier du dépôt
        run: |
          stack="$RUNNER_TEMP/stack"
          mkdir -p "$stack"
          cp docker-compose.yml "$stack/"
          cp .env.example "$stack/.env"
          cd "$stack"
          sed -i 's/^POSTGRES_PASSWORD=.*/POSTGRES_PASSWORD=ci-password/' .env
          docker compose config -q
          docker compose up -d postgres db-backup
          for i in $(seq 1 45); do
            ls backups/cave-*.sql.gz >/dev/null 2>&1 && break
            sleep 2
          done
          docker compose logs db-backup
          ls -la backups/
          dump=$(ls backups/cave-*.sql.gz | head -1)
          gunzip -c "$dump" | grep -q "PostgreSQL database dump"
          # Toujours vivant après le premier dump : le script attend l'intervalle
          # suivant au lieu de sortir (une sortie relancerait la boucle).
          test "$(docker compose ps db-backup --format '{{.State}}')" = running
          test "$(docker inspect --format '{{.RestartCount}}' "$(docker compose ps -q db-backup)")" = 0
      - if: always()
        run: cd "$RUNNER_TEMP/stack" && docker compose down -v
